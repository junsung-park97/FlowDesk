spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flowdesk
    username: son
    password: gudgh
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    show-sql: true